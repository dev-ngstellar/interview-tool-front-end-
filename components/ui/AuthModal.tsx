'use client';

/**
 * AuthModal has been deprecated.
 * Students authenticate via candidate details and OTP verification on the main student portal.
 * Administrators authenticate directly at /admin.
 */
export function AuthModal() {
  return null;
}
